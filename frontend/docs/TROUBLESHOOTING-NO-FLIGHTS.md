# 🔍 แก้ไขปัญหา: ข้อมูลเที่ยวบินไม่ออก

## ⚠️ ปัญหา

เมื่อค้นหาเที่ยวบินแล้วแสดงข้อความ **"ไม่พบเที่ยวบินสำหรับเส้นทางนี้"** หรือไม่มีข้อมูลออกเลย

---

## 🔍 สาเหตุที่เป็นไปได้

### 1. ยังใช้ Mock Data แต่ไม่มีข้อมูล

**ตรวจสอบ:**
- เปิด Browser DevTools → Console
- ดู log ว่าใช้ mock หรือ real data
- ตรวจสอบว่า `NEXT_PUBLIC_USE_MOCK_DATA=true` ใน `.env.local`

**วิธีแก้:**
- Mock data ควรจะมีข้อมูลเสมอ
- ถ้าไม่มีคือมีปัญหาในการ generate mock data
- Restart frontend server

---

### 2. ใช้ Real Data แต่ยังไม่ได้ Import

**ตรวจสอบ:**
```bash
# เช็คว่ามีข้อมูลใน database หรือไม่
docker exec -it flight_search_db psql -U postgres -d flight_search -c "SELECT COUNT(*) FROM flight_prices;"
```

**วิธีแก้:**
```bash
cd backend
npm run generate:mock-data
```

---

### 3. ไม่มีข้อมูลสำหรับเส้นทางที่ค้นหา

**ตรวจสอบ:**
```sql
-- เช็คว่ามีข้อมูลสำหรับเส้นทางที่ค้นหาหรือไม่
SELECT COUNT(*) 
FROM flight_prices fp
INNER JOIN routes r ON fp.route_id = r.id
WHERE r.origin = 'BKK' AND r.destination = 'CNX';
```

**วิธีแก้:**
- Import ข้อมูลสำหรับเส้นทางที่ต้องการ
- หรือลองเส้นทางที่มีข้อมูลมาก เช่น:
  - BKK → CNX (เชียงใหม่)
  - BKK → HKT (ภูเก็ต)

---

### 4. วันที่ที่ค้นหาไม่มีข้อมูล

Import script จะ import ข้อมูลสำหรับ **30-90 วันข้างหน้า** (จากวันนี้)

**ตรวจสอบ:**
```sql
-- เช็คว่ามีข้อมูลในช่วงวันที่ที่ค้นหาหรือไม่
SELECT 
    MIN(departure_date) as earliest,
    MAX(departure_date) as latest
FROM flight_prices;
```

**วิธีแก้:**
- ลองวันที่ที่อยู่ในช่วง 30-90 วันข้างหน้า
- หรือ import ข้อมูลใหม่สำหรับวันที่ที่ต้องการ

---

### 5. Backend API ไม่ทำงาน

**ตรวจสอบ:**
- Backend server ทำงานอยู่หรือไม่ (port 3001)
- เปิด Browser DevTools → Network tab
- ดู error ใน Console

**วิธีแก้:**
```bash
cd backend
npm run dev
```

---

### 6. Environment Variables ไม่ถูกต้อง

**ตรวจสอบ:**
- เปิด Browser DevTools → Console
- ดู log ว่าใช้ mock หรือ real data

**สำหรับ Real Data:**
```env
NEXT_PUBLIC_USE_MOCK_DATA=false
NEXT_PUBLIC_API_URL=http://localhost:3001/api
```

**สำหรับ Mock Data:**
```env
NEXT_PUBLIC_USE_MOCK_DATA=true
```

---

## 🔧 วิธี Debug

### 1. เปิด Browser DevTools

1. กด `F12` หรือ `Ctrl+Shift+I`
2. ไปที่ **Console** tab
3. ดู log messages:
   - `🔍 Loading flights:` - กำลังโหลดข้อมูล
   - `📦 Using MOCK data` - ใช้ mock data
   - `📊 API returned X flights` - ได้ข้อมูลจาก API
   - `❌ Error loading flights:` - มี error

### 2. ดู Network Requests

1. ไปที่ **Network** tab
2. ค้นหาเที่ยวบิน
3. ดูว่ามี request ไปที่ `/api/flights/prices` หรือไม่
4. ดู response status (200 = success, 404/500 = error)

### 3. ตรวจสอบ Database

```bash
# เช็คข้อมูลทั้งหมด
docker exec -it flight_search_db psql -U postgres -d flight_search

# ใน psql:
SELECT COUNT(*) FROM flight_prices;
SELECT * FROM flight_prices LIMIT 5;
```

---

## ✅ Checklist

### สำหรับ Mock Data:
- [ ] `NEXT_PUBLIC_USE_MOCK_DATA=true` ใน `.env.local`
- [ ] Restart frontend server
- [ ] ตรวจสอบ Console - ควรเห็น "📦 Using MOCK data"
- [ ] ตรวจสอบว่า mock data generator ทำงาน

### สำหรับ Real Data:
- [ ] Generate ข้อมูลเที่ยวบินแล้ว
- [ ] Backend server ทำงานอยู่ (port 3001)
- [ ] `NEXT_PUBLIC_USE_MOCK_DATA=false` ใน `.env.local`
- [ ] `NEXT_PUBLIC_API_URL` ตั้งค่าถูกต้อง
- [ ] มีข้อมูลใน database (เช็คด้วย SQL)
- [ ] วันที่ที่ค้นหาอยู่ในช่วงที่มีข้อมูล

---

## 🚀 Quick Fix

### ถ้าใช้ Mock Data:

1. เปิด Browser DevTools → Console
2. ดู error messages
3. Restart frontend server

### ถ้าใช้ Real Data:

1. **Import ข้อมูล:**
   ```bash
   cd backend
   npm run generate:mock-data
   ```

2. **เช็คว่ามีข้อมูล:**
   ```bash
   docker exec -it flight_search_db psql -U postgres -d flight_search -c "SELECT COUNT(*) FROM flight_prices;"
   ```

3. **ถ้ายังไม่มีข้อมูล:**
   - รอให้ import เสร็จ (ใช้เวลา ~15-20 นาที)
   - หรือลองเส้นทางอื่นที่อาจมีข้อมูล

---

**ถ้ายังแก้ไม่ได้ ให้เช็ค Console logs และบอกข้อความ error ที่เจอ** 🔍

