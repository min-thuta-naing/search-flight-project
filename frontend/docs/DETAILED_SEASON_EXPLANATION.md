# การปรับ Season ให้ละเอียดขึ้น

## สถานะปัจจุบัน (แบบง่าย)

ตอนนี้ระบบแบ่ง Season เป็น **3 ระดับ** ตามเดือน:

```
High Season:   พฤศจิกายน - กุมภาพันธ์ (ทั้งเดือน)
Normal Season: มีนาคม - เมษายน (ทั้งเดือน)
Low Season:    พฤษภาคม - ตุลาคม (ทั้งเดือน)
```

### ปัญหาของวิธีปัจจุบัน

**ตัวอย่างปัญหา:**

1. **ธันวาคม 25-31 และมกราคม 1-3 (ปีใหม่)**
   - ตอนนี้: High Season (1.1-1.5x)
   - แต่จริงๆ: ควรเป็น **High Peak** (1.8-2.0x) เพราะเป็นช่วงที่แพงที่สุดของปี

2. **เมษายน 13-15 (สงกรานต์)**
   - ตอนนี้: Normal Season (0.85-1.1x)
   - แต่จริงๆ: ควรเป็น **High Peak** (1.6-1.8x) เพราะเป็นเทศกาลใหญ่

3. **เมษายน 1-12 (ก่อนสงกรานต์)**
   - ตอนนี้: Normal Season (0.85-1.1x)
   - แต่จริงๆ: อาจเป็น **Normal-High** (1.0-1.2x) เพราะคนเริ่มจองก่อนเทศกาล

4. **เมษายน 16-30 (หลังสงกรานต์)**
   - ตอนนี้: Normal Season (0.85-1.1x)
   - แต่จริงๆ: อาจเป็น **Normal** (0.9-1.0x) เพราะผ่านเทศกาลแล้ว

---

## วิธีปรับให้ละเอียดขึ้น

### แบ่งเป็น 5-6 ระดับ แทน 3 ระดับ

```
High Peak    → ราคาแพงที่สุด (เทศกาลใหญ่)
High         → ราคาแพง (High Season ปกติ)
Normal-High  → ราคาปานกลาง-แพง (ก่อนเทศกาล)
Normal       → ราคาปานกลาง
Low          → ราคาถูก
```

### ตัวอย่างการแบ่งที่ละเอียดขึ้น

```typescript
const detailedSeasons = {
  // High Peak - เทศกาลใหญ่ (ราคาแพงที่สุด)
  'high-peak': [
    { month: 11, days: [25, 26, 27, 28, 29, 30, 31] }, // ธันวาคม 25-31 (ปีใหม่)
    { month: 0, days: [1, 2, 3] },                      // มกราคม 1-3 (ปีใหม่)
    { month: 3, days: [13, 14, 15] },                   // เมษายน 13-15 (สงกรานต์)
  ],
  
  // High - High Season ปกติ (ราคาแพง)
  'high': [
    { month: 10, days: [1, 2, 3, ..., 30] },           // พฤศจิกายน ทั้งเดือน
    { month: 11, days: [1, 2, 3, ..., 24] },            // ธันวาคม 1-24
    { month: 0, days: [4, 5, 6, ..., 31] },            // มกราคม 4-31
    { month: 1, days: [1, 2, 3, ..., 28] },            // กุมภาพันธ์ ทั้งเดือน
  ],
  
  // Normal-High - ก่อนเทศกาล (ราคาปานกลาง-แพง)
  'normal-high': [
    { month: 2, days: [1, 2, 3, ..., 31] },            // มีนาคม ทั้งเดือน
    { month: 3, days: [1, 2, 3, ..., 12] },            // เมษายน 1-12 (ก่อนสงกรานต์)
  ],
  
  // Normal - ช่วงกลาง (ราคาปานกลาง)
  'normal': [
    { month: 3, days: [16, 17, 18, ..., 30] },         // เมษายน 16-30 (หลังสงกรานต์)
  ],
  
  // Low - Low Season (ราคาถูก)
  'low': [
    { month: 4, days: [1, 2, 3, ..., 31] },            // พฤษภาคม ทั้งเดือน
    { month: 5, days: [1, 2, 3, ..., 30] },            // มิถุนายน ทั้งเดือน
    { month: 6, days: [1, 2, 3, ..., 31] },            // กรกฎาคม ทั้งเดือน
    { month: 7, days: [1, 2, 3, ..., 31] },           // สิงหาคม ทั้งเดือน
    { month: 8, days: [1, 2, 3, ..., 30] },            // กันยายน ทั้งเดือน
    { month: 9, days: [1, 2, 3, ..., 31] },           // ตุลาคม ทั้งเดือน
  ],
}
```

---

## เปรียบเทียบ: วิธีปัจจุบัน vs วิธีละเอียด

### ตัวอย่าง: วันที่ 13 เมษายน (สงกรานต์)

**วิธีปัจจุบัน:**
```
เมษายน = Normal Season
ราคา = basePrice × 0.975 (ค่าเฉลี่ยของ Normal Season)
```

**วิธีละเอียด:**
```
เมษายน 13 = High Peak (สงกรานต์)
ราคา = basePrice × 1.8 (High Peak)
```

**ผลลัพธ์:** ราคาเพิ่มขึ้น ~85% (จาก 0.975x เป็น 1.8x)

---

### ตัวอย่าง: วันที่ 31 ธันวาคม (ปีใหม่)

**วิธีปัจจุบัน:**
```
ธันวาคม = High Season
ราคา = basePrice × 1.5 (max ของ High Season)
```

**วิธีละเอียด:**
```
ธันวาคม 31 = High Peak (ปีใหม่)
ราคา = basePrice × 2.0 (High Peak)
```

**ผลลัพธ์:** ราคาเพิ่มขึ้น ~33% (จาก 1.5x เป็น 2.0x)

---

## ข้อดีของการปรับให้ละเอียดขึ้น

### ✅ 1. ราคาสะท้อนความเป็นจริงมากขึ้น
- เทศกาลใหญ่ (สงกรานต์, ปีใหม่) ราคาแพงมาก
- ช่วงก่อนเทศกาล ราคาเริ่มสูงขึ้น
- ช่วงหลังเทศกาล ราคาลดลง

### ✅ 2. แม่นยำกว่า
- แบ่งตามวันที่ ไม่ใช่ทั้งเดือน
- พิจารณาเทศกาลและวันหยุดสำคัญ
- สะท้อนพฤติกรรมการจองจริง

### ✅ 3. เหมือนเว็บไซต์ใหญ่ๆ
- Trip.com, Traveloka ใช้วิธีนี้
- แบ่งเป็นหลายระดับ (Peak, High, Normal, Low)
- ราคาเปลี่ยนตามช่วงเวลาที่ละเอียด

---

## ข้อเสีย/ความท้าทาย

### ⚠️ 1. ซับซ้อนขึ้น
- ต้องกำหนดช่วงวันที่หลายช่วง
- ต้องจัดการกับปีที่ต่างกัน (ปีใหม่ข้ามปี)
- ต้องอัปเดตทุกปี

### ⚠️ 2. ต้องดูแลมากขึ้น
- เทศกาลบางเทศกาลเปลี่ยนทุกปี (เช่น ลอยกระทง)
- วันหยุดราชการอาจเปลี่ยน
- ต้องตรวจสอบและอัปเดตเป็นประจำ

---

## สรุป

**การปรับ Season ให้ละเอียดขึ้น** หมายถึง:

1. **เพิ่มระดับ Season** จาก 3 ระดับ → 5-6 ระดับ
   - High Peak, High, Normal-High, Normal, Low

2. **แบ่งตามวันที่** แทนทั้งเดือน
   - 13-15 เมษายน = High Peak (สงกรานต์)
   - 25-31 ธันวาคม = High Peak (ปีใหม่)

3. **พิจารณาเทศกาล** มากขึ้น
   - เทศกาลใหญ่ = High Peak
   - ก่อนเทศกาล = Normal-High
   - หลังเทศกาล = Normal

4. **ราคาสะท้อนความเป็นจริง** มากขึ้น
   - เทศกาลใหญ่ ราคาแพงมาก
   - ช่วงปกติ ราคาปกติ

---

## ตัวอย่างโค้ดที่ควรจะเป็น

```typescript
// ตรวจสอบว่าเป็น High Peak หรือไม่
function isHighPeak(date: Date): boolean {
  const month = date.getMonth()
  const day = date.getDate()
  
  // ปีใหม่
  if (month === 11 && day >= 25) return true // ธันวาคม 25-31
  if (month === 0 && day <= 3) return true   // มกราคม 1-3
  
  // สงกรานต์
  if (month === 3 && day >= 13 && day <= 15) return true // เมษายน 13-15
  
  return false
}

// ตรวจสอบว่าเป็น High Season หรือไม่
function isHighSeason(date: Date): boolean {
  if (isHighPeak(date)) return false // High Peak ไม่ใช่ High
  
  const month = date.getMonth()
  // พฤศจิกายน, ธันวาคม 1-24, มกราคม 4-31, กุมภาพันธ์
  return month === 10 || 
         (month === 11 && date.getDate() <= 24) ||
         (month === 0 && date.getDate() >= 4) ||
         month === 1
}

// ตรวจสอบว่าเป็น Normal-High หรือไม่
function isNormalHigh(date: Date): boolean {
  const month = date.getMonth()
  const day = date.getDate()
  
  // มีนาคม ทั้งเดือน
  if (month === 2) return true
  
  // เมษายน 1-12 (ก่อนสงกรานต์)
  if (month === 3 && day <= 12) return true
  
  return false
}
```

---

## คำแนะนำ

### สำหรับตอนนี้
- ✅ ใช้วิธีปัจจุบันได้ (3 ระดับ) เพราะมี Pricing Factors ช่วยแล้ว
- ✅ Pricing Factors จะจัดการเทศกาลให้ (สงกรานต์ +60%, ปีใหม่ +50%)

### สำหรับอนาคต
- 🔄 ถ้าต้องการความแม่นยำมากขึ้น → ปรับ Season ให้ละเอียดขึ้น
- 🔄 ถ้าต้องการง่าย → ใช้วิธีปัจจุบัน + Pricing Factors

