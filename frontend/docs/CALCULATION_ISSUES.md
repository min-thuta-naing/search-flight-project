# ปัญหาการคำนวณที่พบในระบบ

## 1. ❌ Price Comparison ใช้ selectedAirlines แทน allAirlines

**ปัญหา:**
- `priceComparison` (ถ้าคุณไปก่อน/หลัง) ใช้ `selectedAirlines` ในการคำนวณราคา
- แต่ `bestDeal` ใช้ `allAirlines` ในการคำนวณ
- ทำให้ราคาไม่สอดคล้องกัน

**ตำแหน่ง:** `lib/flight-analysis.ts` บรรทัด 283, 316, 335

**ผลกระทบ:**
- เมื่อ filter สายการบิน ราคา "ถ้าคุณไปก่อน/หลัง" เปลี่ยนตาม
- แต่ราคา recommended period ไม่เปลี่ยน (เพราะใช้ allAirlines)
- ทำให้เปรียบเทียบราคาไม่ถูกต้อง

**วิธีแก้:**
- ใช้ `allAirlines` แทน `selectedAirlines` ในการคำนวณ price comparison

---

## 2. ❌ Recommended Period Price ไม่สอดคล้องกับ Best Deal

**ปัญหา:**
- `recommendedPeriod.price` ใช้ `baseRecommendedPrice` ซึ่งคำนวณจากวันที่ที่เลือกจริง
- แต่ `recommendedPeriod.airline` และ `recommendedPeriod.season` ใช้จาก `bestDeal`
- ทำให้ข้อมูลไม่สอดคล้องกัน

**ตำแหน่ง:** `lib/flight-analysis.ts` บรรทัด 396-403

**ผลกระทบ:**
- ถ้าเลือกวันที่ใน High Season แต่ bestDeal เป็น Low Season
- `recommendedPeriod.price` จะเป็นราคา High Season
- แต่ `recommendedPeriod.season` จะเป็น 'low'
- ทำให้ข้อมูลสับสน

**วิธีแก้:**
- ใช้ `bestDeal.bestDeal.price` แทน `baseRecommendedPrice` สำหรับ recommendedPeriod
- หรือแยก recommendedPeriod ออกเป็น 2 แบบ:
  - แบบที่ 1: ใช้ bestDeal (ราคาถูกที่สุด)
  - แบบที่ 2: ใช้ราคาของวันที่ที่เลือกจริง

---

## 3. ❌ Savings Calculation ไม่ถูกต้อง

**ปัญหา:**
- `savings` คำนวณจาก `baseHighSeasonPrice - baseRecommendedPrice`
- แต่ `baseRecommendedPrice` เป็นราคาของวันที่ที่เลือกจริง (อาจไม่ใช่ Low Season)
- ทำให้ savings อาจติดลบหรือไม่ถูกต้อง

**ตำแหน่ง:** `lib/flight-analysis.ts` บรรทัด 403

**ผลกระทบ:**
- ถ้าเลือกวันที่ใน High Season
- `baseRecommendedPrice` จะเป็นราคา High Season
- `savings` จะเป็น `baseHighSeasonPrice - baseHighSeasonPrice = 0` หรือติดลบ
- ซึ่งไม่ถูกต้อง เพราะควรเปรียบเทียบกับ High Season price

**วิธีแก้:**
- ใช้ `bestDeal.bestDeal.price` แทน `baseRecommendedPrice` ในการคำนวณ savings
- หรือคำนวณ savings จาก current season price แทน

---

## 4. ❌ Recommended Period Dates ไม่สอดคล้องกับ Price

**ปัญหา:**
- `recommendedPeriod.startDate` และ `recommendedPeriod.endDate` ใช้ `bestDeal.bestDeal.dates`
- แต่ `recommendedPeriod.price` ใช้ `baseRecommendedPrice` (ราคาของวันที่ที่เลือกจริง)
- ทำให้วันที่และราคาไม่สอดคล้องกัน

**ตำแหน่ง:** `lib/flight-analysis.ts` บรรทัด 375-400

**ผลกระทบ:**
- แสดงวันที่ Low Season แต่ราคาเป็น High Season
- ทำให้ผู้ใช้สับสน

**วิธีแก้:**
- ถ้าใช้ bestDeal dates ต้องใช้ bestDeal price ด้วย
- หรือถ้าใช้ราคาของวันที่ที่เลือก ต้องใช้วันที่ที่เลือกด้วย

---

## 5. ⚠️ One-way Price Calculation อาจไม่สอดคล้อง

**ปัญหา:**
- `baseRecommendedPrice` ถูกหาร 2 สำหรับ one-way
- แต่ `bestDeal.bestDeal.price` ใน seasons ไม่ถูกหาร 2
- ทำให้เปรียบเทียบราคาไม่ถูกต้อง

**ตำแหน่ง:** `lib/flight-analysis.ts` บรรทัด 291-293

**ผลกระทบ:**
- เมื่อเปรียบเทียบราคา one-way กับ round-trip อาจไม่สอดคล้อง

**วิธีแก้:**
- ตรวจสอบให้แน่ใจว่าทุกส่วนที่เกี่ยวข้องกับ one-way ถูกหาร 2 อย่างสอดคล้องกัน

---

## 6. ⚠️ Price Comparison สำหรับ One-way

**ปัญหา:**
- `beforePrice` และ `afterPrice` ถูกหาร 2 สำหรับ one-way
- แต่ `baseRecommendedPrice` ก็ถูกหาร 2 แล้ว
- ควรถูกต้อง แต่ต้องตรวจสอบว่า recommendedStartDate ถูกต้องสำหรับ one-way

**ตำแหน่ง:** `lib/flight-analysis.ts` บรรทัด 309-330

**ผลกระทบ:**
- ถ้า recommendedStartDate ไม่ถูกต้องสำหรับ one-way การคำนวณอาจผิดพลาด

---

## สรุปปัญหาเรียงตามความสำคัญ

### 🔴 **สำคัญมาก (ต้องแก้ไขทันที):**
1. Price Comparison ใช้ selectedAirlines แทน allAirlines
2. Recommended Period Price ไม่สอดคล้องกับ Best Deal
3. Savings Calculation ไม่ถูกต้อง

### 🟡 **สำคัญปานกลาง (ควรแก้ไข):**
4. Recommended Period Dates ไม่สอดคล้องกับ Price
5. One-way Price Calculation อาจไม่สอดคล้อง

### 🟢 **สำคัญน้อย (ตรวจสอบ):**
6. Price Comparison สำหรับ One-way

---

## คำแนะนำ

### ทางเลือกที่ 1: ใช้ Best Deal เสมอ
- `recommendedPeriod` ใช้ bestDeal ทั้งหมด (dates, price, airline, season)
- `priceComparison` เปรียบเทียบกับ bestDeal price
- **ข้อดี:** สอดคล้องกัน, ง่ายต่อการเข้าใจ
- **ข้อเสีย:** ไม่สะท้อนราคาของวันที่ที่เลือกจริง

### ทางเลือกที่ 2: ใช้ราคาของวันที่ที่เลือกจริง
- `recommendedPeriod` ใช้วันที่และราคาที่เลือกจริง
- `priceComparison` เปรียบเทียบกับราคาของวันที่ที่เลือก
- **ข้อดี:** สะท้อนความเป็นจริง
- **ข้อเสีย:** ซับซ้อนกว่า, อาจไม่สอดคล้องกับ bestDeal

### ทางเลือกที่ 3: แยกเป็น 2 ส่วน
- ส่วนที่ 1: Best Deal Recommendation (ราคาถูกที่สุด)
- ส่วนที่ 2: Selected Date Price (ราคาของวันที่ที่เลือก)
- **ข้อดี:** ชัดเจน, ไม่สับสน
- **ข้อเสีย:** UI ซับซ้อนขึ้น

