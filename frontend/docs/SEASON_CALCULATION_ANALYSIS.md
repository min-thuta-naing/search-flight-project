# การวิเคราะห์วิธีคำนวณ High/Low/Normal Season

## วิธีคิดในโปรเจคปัจจุบัน

### 1. การแบ่ง Season (Hard-coded ตามเดือน)
```
Low Season:    พฤษภาคม - ตุลาคม (6 เดือน)
Normal Season: มีนาคม - เมษายน (2 เดือน)  
High Season:   พฤศจิกายน - กุมภาพันธ์ (4 เดือน)
```

### 2. Price Multiplier (ค่าคงที่)
```
Low Season:    0.7 - 0.85  (ลด 15-30%)
Normal Season: 0.85 - 1.1  (ปกติถึงเพิ่ม 10%)
High Season:    1.1 - 1.5   (เพิ่ม 10-50%)
```

### 3. การคำนวณราคา
```typescript
ราคา = basePrice × multiplier
- Low Season: ใช้ multiplier.min (0.7)
- High Season: ใช้ multiplier.max (1.5)
- Normal Season: ใช้ค่าเฉลี่ย (0.975)
```

---

## วิธีคิดของเว็บไซต์ใหญ่ๆ (Trip.com, Traveloka, Skyscanner)

### 1. ใช้ข้อมูลจริงจาก API
- **Google Flights API**: ดึงราคาจริงจากสายการบิน
- **Flight Price API**: ข้อมูลราคาและความพร้อม
- **Sabre API**: ข้อมูลการจองและราคา
- **อัปเดต Real-time**: ราคาเปลี่ยนตามความต้องการจริง

### 2. วิเคราะห์ข้อมูลย้อนหลัง (Historical Data Analysis)
```
- เก็บข้อมูลราคา 12-24 เดือนย้อนหลัง
- คำนวณค่าเฉลี่ย (Mean), Median, Percentile
- วิเคราะห์ Pattern: วันไหน/เดือนไหนราคาแพง/ถูก
- เปรียบเทียบกับปีก่อนหน้า
```

### 3. พิจารณาปัจจัยหลายอย่าง

#### 3.1 เทศกาลและวันหยุด
```
- วันหยุดยาว (สงกรานต์, ปีใหม่, วันหยุดราชการ)
- เทศกาลสำคัญ (ลอยกระทง, วันวาเลนไทน์)
- ปิดเทอม (มีนาคม-เมษายน, ตุลาคม-พฤศจิกายน)
- วันหยุดสุดสัปดาห์ (ศุกร์-อาทิตย์)
```

#### 3.2 สภาพอากาศและฤดูกาล
```
- ฤดูหนาว (พฤศจิกายน-กุมภาพันธ์): อากาศดี → High Season
- ฤดูร้อน (มีนาคม-พฤษภาคม): ร้อน → Normal/Low
- ฤดูฝน (มิถุนายน-ตุลาคม): ฝนตก → Low Season
```

#### 3.3 ความต้องการ (Demand)
```
- จำนวนการค้นหา (Search Volume)
- จำนวนการจอง (Booking Rate)
- Load Factor ของเที่ยวบิน
- การแข่งขันระหว่างสายการบิน
```

#### 3.4 การจองล่วงหน้า (Advance Booking)
```
- จองล่วงหน้า 2-3 เดือน: ราคาถูก
- จองล่วงหน้า 1 เดือน: ราคาปกติ
- จองล่วงหน้า 1-2 สัปดาห์: ราคาแพง
- จองใกล้วันเดินทาง: ราคาแพงมาก
```

#### 3.5 วันในสัปดาห์ (Day of Week)
```
- วันจันทร์-พฤหัสบดี: ราคาถูก (Business Travel)
- วันศุกร์-อาทิตย์: ราคาแพง (Leisure Travel)
- วันอังคาร-พุธ: ราคาถูกที่สุด
```

### 4. ใช้ Machine Learning / AI
```
- Predictive Analytics: ทำนายราคาในอนาคต
- Dynamic Pricing: ปรับราคาตามความต้องการ
- Price Optimization: หาจุดราคาที่เหมาะสม
- Demand Forecasting: ทำนายความต้องการ
```

### 5. การคำนวณแบบ Dynamic
```typescript
// ตัวอย่างการคำนวณแบบ Real-world
function calculatePrice(basePrice, factors) {
  let multiplier = 1.0
  
  // ปัจจัยตาม Season
  if (isHighSeason(date)) multiplier *= 1.3
  else if (isLowSeason(date)) multiplier *= 0.75
  
  // ปัจจัยตามวันหยุด
  if (isHoliday(date)) multiplier *= 1.2
  
  // ปัจจัยตามวันในสัปดาห์
  if (isWeekend(date)) multiplier *= 1.15
  
  // ปัจจัยตามการจองล่วงหน้า
  const daysAhead = daysUntil(date)
  if (daysAhead < 7) multiplier *= 1.5
  else if (daysAhead < 30) multiplier *= 1.2
  
  // ปัจจัยตามความต้องการ
  const demandFactor = getDemandFactor(route, date)
  multiplier *= demandFactor
  
  return basePrice * multiplier
}
```

---

## เปรียบเทียบ: โปรเจคปัจจุบัน vs เว็บไซต์ใหญ่ๆ

| หัวข้อ | โปรเจคปัจจุบัน (เดิม) | โปรเจคปัจจุบัน (อัปเดตแล้ว) | เว็บไซต์ใหญ่ๆ |
|--------|----------------------|---------------------------|---------------|
| **ข้อมูล** | Mock Data (Hard-coded) | Mock Data (Hard-coded) | Real-time API Data |
| **การแบ่ง Season** | ตามเดือน (คงที่) | ตามเดือน (คงที่) | ตามหลายปัจจัย (Dynamic) |
| **Price Multiplier** | ค่าคงที่ (0.7-1.5) | ค่าคงที่ + Pricing Factors | คำนวณจากข้อมูลจริง |
| **พิจารณาเทศกาล** | ❌ ไม่มี | ✅ **มีแล้ว** (สงกรานต์, ปีใหม่, ฯลฯ) | ✅ มี (สงกรานต์, ปีใหม่, ฯลฯ) |
| **พิจารณาวันหยุด** | ❌ ไม่มี | ✅ **มีแล้ว** (วันหยุดราชการ, วันหยุดสุดสัปดาห์) | ✅ มี (วันหยุดราชการ, วันหยุดสุดสัปดาห์) |
| **พิจารณาการจองล่วงหน้า** | ❌ ไม่มี | ✅ **มีแล้ว** (ยิ่งจองใกล้ยิ่งแพง) | ✅ มี (ยิ่งจองใกล้ยิ่งแพง) |
| **พิจารณาวันในสัปดาห์** | ❌ ไม่มี | ✅ **มีแล้ว** (ศุกร์-อาทิตย์แพงกว่า) | ✅ มี (ศุกร์-อาทิตย์แพงกว่า) |
| **อัปเดต Real-time** | ❌ ไม่มี | ❌ ไม่มี | ✅ มี (ราคาเปลี่ยนตามความต้องการ) |
| **Machine Learning** | ❌ ไม่มี | ❌ ไม่มี | ✅ มี (ทำนายราคา) |

---

## สิ่งที่ทำไปแล้ว ✅

### 1. ✅ สร้างระบบ Pricing Factors
- **ไฟล์**: `services/pricing-factors.ts`
- **ฟีเจอร์**:
  - ✅ เทศกาล/วันหยุดในไทย (10+ เทศกาล)
    - สงกรานต์: +60%
    - ปีใหม่: +50%
    - วันหยุดอื่นๆ: +10-30%
  - ✅ วันในสัปดาห์
    - อังคาร-พุธ: -15% (ถูกที่สุด)
    - จันทร์-พฤหัสบดี: -10%
    - ศุกร์-เสาร์-อาทิตย์: +15-20%
  - ✅ การจองล่วงหน้า
    - 3 เดือนขึ้นไป: -15%
    - 2-3 เดือน: -10%
    - 1-2 เดือน: -5%
    - 2 สัปดาห์-1 เดือน: ปกติ
    - 1-2 สัปดาห์: +20%
    - 3-7 วัน: +40%
    - 0-3 วัน: +60%

### 2. ✅ ผสาน Pricing Factors เข้ากับโค้ด
- **ไฟล์ที่แก้ไข**:
  - `lib/flight-analysis.ts`
    - ✅ เพิ่ม `calculatePricingFactors` ใน `getCheapestAirlineForSeason`
    - ✅ เพิ่ม `calculatePriceWithFactors` ใน `generateChartData`
    - ✅ อัปเดต bestDeal ให้ใช้ pricing factors
  - `services/mock-flights.ts`
    - ✅ อัปเดตการคำนวณราคาเที่ยวบินให้ใช้ pricing factors

### 3. ✅ วิธีคำนวณราคาใหม่
```typescript
// ราคาสุดท้าย = basePrice × seasonMultiplier × pricingFactors
// pricingFactors = holidayMultiplier × dayOfWeekMultiplier × advanceBookingMultiplier

// ตัวอย่าง: วันที่ 13 เมษายน (สงกรานต์, อาทิตย์), จองล่วงหน้า 1 เดือน
// ราคา = basePrice × 0.75 (Low Season) × 1.6 (สงกรานต์) × 1.2 (อาทิตย์) × 0.95 (จองล่วงหน้า)
//      = basePrice × 1.368 (เพิ่ม 36.8%)
```

### 4. ✅ เอกสารประกอบ
- **ไฟล์**: `docs/PRICING_FACTORS_GUIDE.md`
- **เนื้อหา**: คู่มือการใช้งาน pricing factors พร้อมตัวอย่าง

---

## ปัญหาที่เหลืออยู่

### 1. ❌ ไม่ได้ใช้ข้อมูลจริง
- ราคาเป็น Mock Data ไม่สะท้อนราคาจริง
- ไม่สามารถเชื่อมต่อกับ API จริงได้

### 2. ⚠️ Multiplier คงที่ (แต่มี Pricing Factors แล้ว)
- Season multiplier ยังคงที่ แต่ตอนนี้มี pricing factors เพิ่มเติมแล้ว
- ไม่ได้พิจารณา Load Factor ของเที่ยวบิน

### 3. ❌ ไม่มี Dynamic Pricing
- ราคาไม่เปลี่ยนตามความต้องการจริง
- ไม่ได้พิจารณาการแข่งขันระหว่างสายการบิน

---

## ข้อเสนอแนะสำหรับการปรับปรุง

### 1. ✅ เพิ่มปัจจัยในการคำนวณ - **ทำเสร็จแล้ว**
```typescript
// ✅ เพิ่มการพิจารณาเทศกาล - ทำเสร็จแล้ว
const holidayMultipliers = {
  'สงกรานต์': 1.6,      // 13-15 เมษายน (+60%)
  'ปีใหม่': 1.5,        // 31 ธันวาคม - 2 มกราคม (+50%)
  'วันหยุดยาว': 1.3,    // วันหยุดราชการ (+30%)
  'วันหยุดสุดสัปดาห์': 1.15  // ศุกร์-อาทิตย์ (+15%)
}

// ✅ เพิ่มการพิจารณาการจองล่วงหน้า - ทำเสร็จแล้ว
const advanceBookingMultipliers = {
  '3 เดือนขึ้นไป': 0.85,  // จองล่วงหน้านาน ราคาถูก (-15%)
  '2-3 เดือน': 0.9,       // จองล่วงหน้านาน ราคาถูก (-10%)
  '1-2 เดือน': 0.95,      // จองล่วงหน้าปกติ ราคาถูกลง (-5%)
  '2 สัปดาห์-1 เดือน': 1.0, // ราคาปกติ
  '1-2 สัปดาห์': 1.2,     // จองใกล้ ราคาแพง (+20%)
  '3-7 วัน': 1.4,         // จองใกล้มาก ราคาแพงมาก (+40%)
  '0-3 วัน': 1.6          // จองใกล้มาก ราคาแพงที่สุด (+60%)
}
```

### 2. ❌ ปรับ Season ให้ละเอียดขึ้น - **ยังไม่ทำ**
```typescript
// แบ่งตามช่วงเวลาที่ละเอียดขึ้น
const detailedSeasons = {
  'high-peak': ['ธันวาคม 25-31', 'มกราคม 1-3'],      // ช่วงปีใหม่
  'high': ['พฤศจิกายน', 'ธันวาคม 1-24', 'มกราคม 4-31', 'กุมภาพันธ์'],
  'normal-high': ['มีนาคม', 'เมษายน 1-12'],          // ก่อนสงกรานต์
  'high-peak': ['เมษายน 13-15'],                     // สงกรานต์
  'normal': ['เมษายน 16-30'],
  'low': ['พฤษภาคม', 'มิถุนายน', 'กรกฎาคม', 'สิงหาคม', 'กันยายน', 'ตุลาคม']
}
```

### 3. ❌ เชื่อมต่อ API จริง - **ยังไม่ทำ**
```typescript
// ใช้ Google Flights API หรือ Flight Price API
async function getRealTimePrice(origin, destination, date) {
  const response = await fetch(
    `https://api.google.com/flights?origin=${origin}&destination=${destination}&date=${date}`
  )
  const data = await response.json()
  return data.price
}
```

### 4. ❌ ใช้ Machine Learning - **ยังไม่ทำ**
```typescript
// ใช้ข้อมูลย้อนหลังทำนายราคา
function predictPrice(route, date, historicalData) {
  // ใช้ Linear Regression หรือ Neural Network
  // ทำนายราคาจากปัจจัยต่างๆ
  return predictedPrice
}
```

---

## สรุป

### ✅ ข้อดีของโปรเจคปัจจุบัน
- ง่ายต่อการเข้าใจและทดสอบ
- ใช้ได้ทันทีโดยไม่ต้องเชื่อมต่อ API
- เหมาะสำหรับ Mock Data / Prototype

### ❌ ข้อเสียของโปรเจคปัจจุบัน
- ❌ ไม่สะท้อนราคาจริง (ยังใช้ Mock Data)
- ✅ พิจารณาปัจจัยสำคัญแล้ว (เทศกาล, วันหยุด, การจองล่วงหน้า) - **ทำเสร็จแล้ว**
- ❌ ไม่มี Dynamic Pricing (ยังไม่ปรับตามความต้องการจริง)

### 🎯 วิธีคิดที่ถูกต้อง (ตามเว็บไซต์ใหญ่ๆ)
1. ❌ **ใช้ข้อมูลจริง** จาก API - **ยังไม่ทำ**
2. ✅ **พิจารณาหลายปัจจัย**: เทศกาล, วันหยุด, วันในสัปดาห์, การจองล่วงหน้า - **ทำเสร็จแล้ว**
3. ❌ **Dynamic Pricing**: ปรับราคาตามความต้องการ - **ยังไม่ทำ**
4. ❌ **Machine Learning**: ทำนายราคาจากข้อมูลย้อนหลัง - **ยังไม่ทำ**
5. ❌ **Real-time Update**: อัปเดตราคาตามความต้องการจริง - **ยังไม่ทำ**

---

## สรุปการพัฒนาที่ทำไปแล้ว

### ✅ สิ่งที่ทำเสร็จแล้ว

1. **สร้างระบบ Pricing Factors** (`services/pricing-factors.ts`)
   - ✅ เทศกาล/วันหยุดในไทย (10+ เทศกาล)
   - ✅ วันในสัปดาห์ (7 วัน)
   - ✅ การจองล่วงหน้า (7 ช่วง)

2. **ผสานเข้ากับโค้ด**
   - ✅ `lib/flight-analysis.ts` - อัปเดตการคำนวณราคา
   - ✅ `services/mock-flights.ts` - อัปเดตราคาเที่ยวบิน

3. **เอกสารประกอบ**
   - ✅ `docs/PRICING_FACTORS_GUIDE.md` - คู่มือการใช้งาน

### 📊 ผลลัพธ์

ตอนนี้ระบบคำนวณราคาโดยพิจารณา:
- ✅ **Season Multiplier**: Low (0.7-0.85), Normal (0.85-1.1), High (1.1-1.5)
- ✅ **Holiday Multiplier**: เทศกาล/วันหยุด (+10-60%)
- ✅ **Day of Week Multiplier**: วันในสัปดาห์ (-15% ถึง +20%)
- ✅ **Advance Booking Multiplier**: การจองล่วงหน้า (-15% ถึง +60%)

**สูตรคำนวณราคา:**
```
ราคาสุดท้าย = basePrice × seasonMultiplier × holidayMultiplier × dayOfWeekMultiplier × advanceBookingMultiplier
```

---

## คำแนะนำสำหรับโปรเจคนี้

### ✅ สำหรับตอนนี้ (Mock Data) - **ทำเสร็จแล้ว**
- ✅ เพิ่มการพิจารณาเทศกาล/วันหยุด
- ✅ เพิ่มการพิจารณาวันในสัปดาห์
- ✅ เพิ่มการพิจารณาการจองล่วงหน้า

### ❌ สำหรับอนาคต (Production) - **ยังไม่ทำ**
- ❌ เชื่อมต่อกับ Google Flights API หรือ Flight Price API
- ❌ เก็บข้อมูลราคาย้อนหลังเพื่อวิเคราะห์
- ❌ ใช้ Machine Learning ทำนายราคา
- ❌ อัปเดตราคา Real-time
- ❌ ปรับ multiplier ตามข้อมูลจริง (แทนค่าคงที่)

---

## ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: สงกรานต์ (High Demand)
```typescript
// วันที่: 13 เมษายน 2025 (สงกรานต์, อาทิตย์)
// จองล่วงหน้า: 1 เดือน
// Season: Low Season (เมษายนอยู่ในช่วง Normal แต่สงกรานต์เป็น High)

const factors = calculatePricingFactors(bookingDate, travelDate)
// {
//   holidayMultiplier: 1.6,        // สงกรานต์ (+60%)
//   dayOfWeekMultiplier: 1.2,     // อาทิตย์ (+20%)
//   advanceBookingMultiplier: 0.95, // จองล่วงหน้า 1 เดือน (-5%)
//   totalMultiplier: 1.824        // รวม = +82.4%
// }

// ราคา = basePrice × 0.75 (Low Season) × 1.824 = basePrice × 1.368
```

### ตัวอย่างที่ 2: วันธรรมดา (Low Demand)
```typescript
// วันที่: 15 มิถุนายน 2025 (อังคาร, ไม่ใช่วันหยุด)
// จองล่วงหน้า: 2 เดือน
// Season: Low Season

const factors = calculatePricingFactors(bookingDate, travelDate)
// {
//   holidayMultiplier: 1.0,        // ไม่ใช่วันหยุด
//   dayOfWeekMultiplier: 0.85,    // อังคาร (-15%)
//   advanceBookingMultiplier: 0.9, // จองล่วงหน้า 2 เดือน (-10%)
//   totalMultiplier: 0.765        // รวม = -23.5%
// }

// ราคา = basePrice × 0.7 (Low Season min) × 0.765 = basePrice × 0.536
```

---

## ไฟล์ที่เกี่ยวข้อง

- `services/pricing-factors.ts` - ระบบ Pricing Factors
- `lib/flight-analysis.ts` - การคำนวณราคา (อัปเดตแล้ว)
- `services/mock-flights.ts` - Mock flight data (อัปเดตแล้ว)
- `docs/PRICING_FACTORS_GUIDE.md` - คู่มือการใช้งาน

