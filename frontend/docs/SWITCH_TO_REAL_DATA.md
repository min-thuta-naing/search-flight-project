# 📥 คู่มือการเปลี่ยนจาก Mock Data เป็น Real Data

## ✅ สิ่งที่พร้อมแล้ว

### 1. **Backend API Endpoints** ✅
   - ✅ `/api/flights/analyze` - วิเคราะห์ราคาเที่ยวบิน
   - ✅ `/api/flights/prices` - ดึงรายการเที่ยวบิน
   - ✅ `/api/flights/airlines` - ดึงสายการบินที่ใช้ได้

### 2. **Frontend Data Source Abstraction** ✅
   - ✅ `lib/services/data-source.ts` - สลับได้ระหว่าง mock และ real
   - ✅ `lib/services/flight-service.ts` - Service layer
   - ✅ Components ใช้ `FlightService` แล้ว

### 3. **Components ที่อัพเดทแล้ว** ✅
   - ✅ `components/price-analysis.tsx` - ใช้ FlightService แล้ว
   - ✅ `components/airline-flights.tsx` - อัพเดทให้ใช้ API จริงแล้ว

---

## 🚀 ขั้นตอนการเปลี่ยนเป็น Real Data

### ขั้นตอนที่ 1: Generate ข้อมูลเที่ยวบิน

ก่อนอื่นต้องสร้างข้อมูลเที่ยวบินเข้า database:

```bash
cd backend

# รัน generate script
npm run generate:mock-data
```

**เวลาใช้**: ~5-10 นาที

**ผลลัพธ์**: ข้อมูลเที่ยวบินถูกบันทึกลง database แล้ว

---

### ขั้นตอนที่ 2: ตรวจสอบว่า Backend ทำงานอยู่

```bash
cd backend

# ตรวจสอบ database connection
npm run test:db

# เริ่ม backend server
npm run dev
```

Backend จะรันที่ `http://localhost:3001`

---

### ขั้นตอนที่ 3: ตั้งค่า Frontend Environment Variables

แก้ไขไฟล์ `frontend/.env.local`:

```env
# เปลี่ยนจาก true เป็น false เพื่อใช้ข้อมูลจริง
NEXT_PUBLIC_USE_MOCK_DATA=false

# ตั้งค่า backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001/api
```

---

### ขั้นตอนที่ 4: Restart Frontend

```bash
cd frontend

# หยุด dev server (Ctrl+C) แล้วเริ่มใหม่
npm run dev
```

**สำคัญ**: ต้อง restart เพื่อให้ environment variables ใหม่ถูกโหลด

---

### ขั้นตอนที่ 5: ทดสอบ

1. เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
2. ค้นหาเที่ยวบิน
3. ตรวจสอบว่าแสดงข้อมูลจริงจาก database

---

## 📊 สิ่งที่เปลี่ยนแปลง

### ก่อน (Mock Data):
- ข้อมูลเป็น mock/synthetic
- ไม่ต้องมี backend
- ข้อมูลเหมือนเดิมทุกครั้ง

### หลัง (Real Data):
- ข้อมูลจริงที่บันทึกใน database
- ต้องมี backend ทำงาน
- ข้อมูลเปลี่ยนแปลงตามข้อมูลที่ import เข้าไป

---

## 🔍 Components ที่เปลี่ยนไป

### ✅ ใช้ Real Data แล้ว:

1. **`components/price-analysis.tsx`**
   - ใช้ `FlightService.analyzePrices()` 
   - สลับระหว่าง mock/real ตาม env var

2. **`components/airline-flights.tsx`**
   - อัพเดทให้ใช้ `FlightService.getFlightPrices()` แล้ว
   - แสดง loading state
   - แสดง error message ถ้ามีปัญหา

---

## ⚠️ ข้อควรระวัง

### 1. **ข้อมูลยังไม่ครบ**
   - ข้อมูลอาจไม่ครอบคลุมทุก route
   - ถ้าไม่มีข้อมูลใน database จะแสดง "ไม่พบเที่ยวบิน"

### 2. **Error Handling**
   - ถ้า backend ไม่ทำงาน จะแสดง error
   - ตรวจสอบ console สำหรับ error details

### 3. **Environment Variables**
   - ต้อง restart frontend หลังเปลี่ยน `.env.local`
   - ตรวจสอบว่า env vars ถูกโหลดด้วย `console.log(process.env.NEXT_PUBLIC_USE_MOCK_DATA)`

---

## 🔄 กลับไปใช้ Mock Data

ถ้าต้องการกลับไปใช้ mock data:

```env
NEXT_PUBLIC_USE_MOCK_DATA=true
```

แล้ว restart frontend

---

## 📝 Checklist

- [ ] Generate ข้อมูลเที่ยวบินเข้า database
- [ ] Backend server ทำงานอยู่ (port 3001)
- [ ] Database มีข้อมูลเที่ยวบิน
- [ ] ตั้งค่า `NEXT_PUBLIC_USE_MOCK_DATA=false` ใน `.env.local`
- [ ] ตั้งค่า `NEXT_PUBLIC_API_URL` ใน `.env.local`
- [ ] Restart frontend
- [ ] ทดสอบค้นหาเที่ยวบิน

---

## 🐛 Troubleshooting

### Error: "Flight prices API not available"
- ตรวจสอบว่า backend server ทำงานอยู่
- ตรวจสอบว่า `NEXT_PUBLIC_API_URL` ถูกต้อง

### Error: "ไม่พบเที่ยวบิน"
- ตรวจสอบว่า import ข้อมูลเข้า database แล้ว
- ตรวจสอบว่า route ที่ค้นหามีข้อมูลใน database

### Error: Connection refused
- ตรวจสอบว่า backend server ทำงานที่ port 3001
- ตรวจสอบ firewall settings

---

**พร้อมใช้งาน!** 🎉

